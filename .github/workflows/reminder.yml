name: Discord Reminder

on:
  schedule:
    # 5:45 PM hằng ngày (UTC+7 = 10:45 UTC)
    - cron: "45 10 * * *"

    # 9:00 AM Thứ 6 (UTC+7 = 02:00 UTC)
    - cron: "0 2 * * 5"

  workflow_dispatch:

jobs:
  reminder:
    runs-on: ubuntu-latest

    steps:
      - name: Send reminder to Discord
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK }}
          CRON_EXPR: ${{ github.event.schedule }}
        run: |
          if [ "$CRON_EXPR" = "0 2 * * 5" ]; then
            MESSAGE="🚨🚨 **BIG REMIND** 🚨🚨
Hôm nay là **Thứ 6**!
Mọi người nhớ **FULL FILL LOGWORK cho cả tuần** 💪"
          else
            MESSAGE="⏰ **5:45 PM Reminder**
Mọi người nhớ điền **file report** nhé!"
          fi

          jq -n --arg content "$MESSAGE" \
            '{content: $content}' \
            | curl -s -X POST \
                -H "Content-Type: application/json" \
                -d @- \
                "$WEBHOOK_URL"
